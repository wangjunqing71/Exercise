# 字幕拼接长图生成工具需求文档 (PRD)

## 一、产品概述
- 目标：开发一个网页工具，基于用户上传的单张图片与多行文字，生成垂直拼接的长图。长图顶部为原图+第1行文字；其后每一行文字均叠加在“同一段底部背景切片”上，形成连续的“切割感”。
- 典型效果：如示例图，底部出现多行对白，每一行所在的背景相同，并以细线分隔，呈现连贯但被切开的视觉效果。

## 二、核心视觉规则
- 背景一致：除首屏完整图片外，后续每一行文字的背景均取自原图底部的同一高度切片（相同区域、相同宽度与样式）。
- 切割感：在每一行文字区域的上下使用 1px～2px 的分隔线（颜色可配置，默认白色半透明），增强“切片边界”可感知度。
- 文本风格：默认红色填充、白色描边（可配置），居中对齐，位于底部安全区内。
- 水印：支持在右上或右下叠加水印文案或图标（可选）。

## 三、用户流程
1. 上传图片（拖拽/点击）
2. 编辑多行字幕（添加/删除/排序）
3. 设置样式与切片参数（切片高度、分隔线、文字样式等）
4. 预览生成效果（实时）
5. 导出长图（PNG/JPG）或复制到剪贴板

## 四、功能需求
### 4.1 图片上传
- 支持格式：`JPG/PNG/WEBP/GIF(取首帧)`
- 限制：单文件 ≤ 10MB；宽度 ≤ 5000px（超出则自动等比压缩至设定宽度）
- 校验：尺寸、类型、大小提示；失败状态与重试

### 4.2 字幕编辑
- 多行输入：列表形式，支持新增、删除、拖拽排序
- 批量粘贴：支持按行粘贴自动拆分
- 行内设置：可为单行设置“上/下边距”与“行内对齐”（默认继承全局）

### 4.3 样式设置
- 字体：字号、字重、字体族（中文黑体/思源黑体等，回退方案）、字间距、行间距
- 颜色：填充色、描边色与描边宽度；可加阴影（颜色/偏移/模糊）
- 对齐：水平对齐（居中/左/右）、垂直位置（距底部安全区像素）
- 分隔线：开关、颜色、粗细、透明度、上下边距
- 水印：开关、位置（右上/右下）、内容（文本/图片）、不透明度与大小

### 4.4 切片与拼接（核心算法）
- 术语：
  - `Img_raw`：原始上传图片
  - `H_slice`：切片高度（用户可调）
  - `Texts = [T1..Tn]`：字幕行
- 逻辑：
  1) 生成首屏：绘制 `Img_raw` 全图并在底部渲染 `T1`
  2) 生成切片模版：截取 `Img_raw` 底部高 `H_slice` 的区域，得到 `Bg_slice`
  3) 对每个 `Ti (i>=2)`：复制 `Bg_slice`，渲染 `Ti`，可在上下绘制分隔线
  4) 拼接：将首屏与各切片在同一宽度的画布上自上而下依次绘制
- 导出尺寸：`W = Img_raw.width`，`H = Img_raw.height + (n-1) * H_slice`
- 高分屏支持：依据 `devicePixelRatio` 放大画布再等比导出

### 4.5 预览与导出
- 预览：实时渲染，提供“切片高度”拖拽参考线；支持缩放查看
- 导出：PNG（默认）/JPG（质量可选）；支持复制到剪贴板（浏览器支持时）
- 文件名：`{日期}-{随机短码}.png`

## 五、交互与布局
- 左侧控制面板：上传、切片高度滑块、字幕列表、样式设置、分隔线开关、水印设置、导出按钮
- 右侧预览区：居中展示，标注切片参考线；预览宽度自适应，保持比例
- 失败与占位：未上传时显示引导占位图与使用说明

## 六、技术实现建议
- 前端：`React + Vite` 或 `Vue + Vite`
- 样式：`TailwindCSS`（便捷、可主题化）
- 绘制：`Canvas 2D`（文本需自行描边；兼容性佳）
- 状态：`Zustand/Redux` 或 `Pinia`（依据框架）
- 字体：内置 Web 字体与系统字体回退；中文需注意授权与加载时序

## 七、性能与体验
- 防抖：对文本与滑块操作引入 150–300ms 防抖
- 内存：大图按需缩放以控制画布面积；生成后释放中间对象
- 渲染质量：启用 `textBaseline`、`shadow`、`lineJoin`、`miterLimit` 优化文字可读性

## 八、配置项（默认值）
- `H_slice`: 220px（随图片尺寸自适应初始值，可拖拽调整）
- 文本填充色：`#FF4D4F`（红）
- 描边色：`#FFFFFF`，描边宽度：2px
- 对齐：居中；距底部：24px；行间距：1.2em
- 分隔线：开启；颜色 `rgba(255,255,255,0.9)`；粗细 1px；上下边距 12px
- 水印：关闭（用户可开启；默认位置右下）

## 九、边界与兼容
- 极长文本：自动换行，超过切片高度则增大 `H_slice` 或缩小字号并提示
- 宽高极端比例：过宽或过长图片按策略等比压缩至可视范围
- 浏览器：Chrome/Edge/Safari 最新版本；移动端需适配触控与剪贴板权限

## 十、验收标准（Acceptance Criteria）
- 生成长图中，第2行及之后的每一行文字所处背景完全一致（取自原图底部同一切片），肉眼不可见错位
- 分隔线在每个切片的上下边界显示，默认 1px，颜色与透明度生效
- 首屏显示完整原图且正确渲染第1行文字；其余行仅使用切片背景
- 文本描边、颜色、字号、对齐等设置对所有行生效；单行覆盖时仅该行变化
- 导出的图片尺寸与设定一致，像素密度正确，移动端清晰
- 上传异常、超限、导出失败等场景均有明确提示与可重试路径

## 十一、里程碑
- M1：基础上传、切片高度、文本多行渲染与拼接、导出
- M2：样式完整参数、分隔线与水印、预览与拖拽参考线
- M3：性能优化、移动端适配、剪贴板复制与更多字体

## 十二、示例数据
- 文本：
  1) 说吧，你还想要啥？
  2) 都是小 CASE，
  3) 分分钟变给你
- 期望：第二行与第三行的文字背景区域完全一致，并由分隔线分隔，形成明显“切割感”。

